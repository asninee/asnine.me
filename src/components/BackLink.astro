---
import { Icon } from 'astro-icon/components'
import Link from './Link.astro'

const { href } = Astro.props

const destination = href === '/' ? 'home' : 'blog'
---

<Link
  lg
  href={href}
  noUnderline
  label={`Go back to ${destination} page`}
  class='relative group/back w-8 h-5 after:absolute after:inset-y-0 after:-right-1 after:w-[0.5px] after:bg-neutral-400 dark:after:bg-neutral-600 flex items-center justify-center text-primary transition-opacity opacity-60 hover:opacity-100 focus-visible:opacity-100'
  ><Icon
    name='carbon:arrow-left'
    class='w-4 h-4 text-l/sec dark:text-d/sec transition-transform group-hover/back:-translate-x-1'
  />
</Link>
